<!doctype html>
<html lang="fr">

<head>
	<meta charset="utf-8">
	<meta name="description" content="Détail d'une offre de stage - LifePath">
	<link rel="stylesheet" href="/assets/style.css">
	<title>Détail de l'offre - LifePath</title>
</head>

<body>
	{% include 'style_base/_navbar.twig' %}
	{% include 'style_base/_header.twig' %}

	<main class="offer-detail">
		<div class="offer-main-container">
			<div class="offer-left-panel">
				<div class="offer-card">
					<img src="images/entreprise/entreprise.jpg" alt="Offre de stage" class="offer-detail-img">

					<div class="offer-header">
						<h1 class="offer-title">{{ offer.titre }}</h1>
						<p class="offer-reference">{{ offer.entreprise_nom }} <span class="rating">{{ offer.entreprise_note ?? '0' }}/5 {{ (offer.entreprise_note ?? 0) | stars | raw }}</span></p>
					</div>

					<section class="offer-section">
						<h3>Détail de l'offre</h3>
						<div class="offer-detail-row">
							<span class="label">Type de poste:</span>
							<span class="badge badge-blue">Stage</span>
						</div>
						<div class="offer-detail-row">
							<span class="label">Lieu:</span>
							<span class="badge badge-yellow">{{ offer.localisation }}</span>
						</div>
						<div class="offer-detail-row">
							<span class="label">Durée:</span>
							<span class="badge badge-blue">{{ offer.duree }}</span>
						</div>
						<div class="offer-detail-row">
							<span class="label">Salaire mensuel:</span>
							<span class="badge badge-yellow">{{ offer.salaire }}€</span>
						</div>
					</section>

					<section class="offer-section">
						<h3>Description</h3>
						<p class="offer-description">{{ offer.detail | nl2br }}</p>
						<div class="offer-requirements">
							<p>Expérience requise: <span class="highlight">{{ offer.niveau }}</span></p>
							<p>Poste à pourvoir entre le <span class="highlight">{{ offer.date_debut_formatted }}</span> et le <span class="highlight">{{ offer.date_fin_formatted }}</span></p>
						</div>
					</section>

					<div class="offer-action">
						<a href="offres-stage-postuler.html" class="btn-postuler">Postuler</a>
					</div>
				</div>
			</div>

			<!-- Colonne droite: Découvrez l'entreprise jusqu'au bouton voir plus -->
			<div class="offer-right-panel">
				<section class="discover-company">
					<h4>Découvrez l'entreprise :</h4>
					<p>{{ entreprise.description }}</p>
					<span class="arrow">→</span>
				</section>

				<section class="similar-offers stats-section">
					<div class="similar-offer-stats">
						<div class="stat blue">
							<p>300 personnes ont postulé à l'offre</p>
						</div>
						<div class="stat yellow">
							<p>5 offres disponibles chez {{ offer.entreprise_nom }}</p>
							<span class="arrow">→</span>
						</div>
					</div>
				</section>

				<section class="similar-offers">
					<h3>Offres similaires</h3>
					<div class="similar-offers-grid single">
						<div class="similar-offer-list">
							<div class="similar-offer-item">
								<p>Stage dev web</p>
								<span class="arrow large">→</span>
							</div>
							<div class="similar-offer-item">
								<p>Stage dev logiciel</p>
								<span class="arrow large">→</span>
							</div>
							<div class="similar-offer-item">
								<p>Stage analyse de données</p>
								<span class="arrow large">→</span>
							</div>
						</div>
					</div>
					<button class="btn-voir-plus">Voir plus</button>
				</section>
			</div>
		</div>
	</main>

{% include 'style_base/_footer.twig' %}
<script src="/script.js"></script>

</body>
</html>
